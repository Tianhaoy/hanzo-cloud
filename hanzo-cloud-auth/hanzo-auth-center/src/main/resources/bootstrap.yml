spring:
  profiles:
    active: dev
  application:
    name: hanzo-auth-center


#开启SpringBoot Admin的监控
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
